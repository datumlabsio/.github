name: Request a monorepo
description: Several components in one repository — a front end, a dbt project, a pipeline, an install's desired state.
title: "Repo request: "
labels: ["repo-request"]

body:
  - type: markdown
    attributes:
      value: |
        Fill this in and a repository will be created for you — with CI, branch
        protection, ownership and the security gates already set up, and one
        folder per component you tick.

        You will get a comment on this issue when it is done, either way.

        **This is one of two ways to get a repository.** If you would rather drive
        it yourself, run `copier copy gh:datumlabsio/scaffolds <name>` — same
        template, same result, and you answer the questions in your terminal.
        Use whichever you prefer.

        **Only one component?** Use *Request a repository* instead — go back and
        pick it from the list.

  - type: input
    id: repo_name
    attributes:
      label: Repository name
      description: Lowercase, hyphens, no spaces. This becomes the URL.
      placeholder: customer-insights
    validations:
      required: true

  - type: input
    id: repo_description
    attributes:
      label: What is it for?
      description: One line. It becomes the repository description, and it is the first thing anyone reads.
      placeholder: Ingestion and models for the Acme engagement
    validations:
      required: true

  - type: input
    id: owning_team
    attributes:
      label: Owning team
      description: >-
        The GitHub team that owns this, lowercase with hyphens. It is checked
        before anything is created — a name that is not a real team is refused,
        and the refusal lists the teams that are.

        It must also **hold write access** to the repository once it exists.
        GitHub ignores a CODEOWNERS entry naming a team without access, with no
        error and no warning, so the comment you get back says whether it really
        has it.
      placeholder: polaris
    validations:
      required: true

  - type: checkboxes
    id: folders
    attributes:
      label: Which parts?
      description: >-
        One folder each, with its own CI and its own owners. Pick at least one.
        Pick only one of the two web-app rows — there is a single `web-app/`
        folder and it is built one way or the other.
      options:
        - label: gitops
        - label: application
        - label: web-app (Next.js)
        - label: web-app (React + Vite)
        - label: dbt-project
        - label: dlt-pipeline

  - type: markdown
    attributes:
      value: |
        ---
        **That is everything we ask.** Anything else the scaffold needs — the
        warehouse for a dbt project, the source name for a pipeline — is chosen
        for you, and the comment you get back lists every value that was used and
        how to change one.

        **What happens next:** a workflow renders the repository from the
        scaffold, creates it, pushes the first commit, protects `main`, and
        comments here with the link. No approval step — it starts as soon as the
        label lands.
